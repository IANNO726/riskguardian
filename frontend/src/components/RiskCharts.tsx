import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

export default function RiskCharts() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">
          Charts disabled
        </Typography>
        <Typography color="text.secondary">
          Recharts removed successfully.
        </Typography>
      </CardContent>
    </Card>
  );
}

